name: nexus_core_so
description: An ultra-lightweight, zero-dependency, reactive state management framework and observer engine designed for cross-platform apps.
version: 1.0.0
homepage: https://github.com/nexus-core-so/nexus-core-so

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
